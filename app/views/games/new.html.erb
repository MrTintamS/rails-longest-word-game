<div class="container">
  <h1>Le mot le plus long</h1>
  <p>Build the longest word using the following letters:</p>
  <ul>
    <% @letters.each do |letter| %>
      <li><%= letter %></li>
    <% end %>
  </ul>
  <form action="<%= score_path %>" method="post" data-turbo="false">
    <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
    <input type="hidden" name="letters" value="<%= @letters.join(' ') %>">
    <label for="word">Your word:</label>
    <input type="text" name="word" id="word" placeholder="Enter your word" required>
    <button type="submit">Submit</button>
  </form>
</div>
